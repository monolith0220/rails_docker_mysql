<!DOCTYPE html>
<html>
  <head>
    <%# 各ページのタイトルはここにまとめる %>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/head' %>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <%= yield %>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
